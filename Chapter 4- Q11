fn concatenate_c_words(input: &str) -> String {
    input
        .split_whitespace()                
        .filter(|word| word.starts_with('c') || word.starts_with('C')) 
        .collect::<String>()            
}
fn main() {
    let input = "cat can code in C or Rust cleanly";
    let result = concatenate_c_words(input);
    println!("Concatenated result: {}", result);
}
o/p:
Concatenated result: catcancodeCcleanly
