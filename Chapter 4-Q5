fn main(){
    let mut arr = [1,2,3,4,5];
    
    println!(" array before squares is {:?}",arr);
    aos(&mut arr);

    println!(" array after squares is {:?}",arr);
}
fn aos(arr: &mut[i32]){
    for i in 0..arr.len(){
        arr[i]*=arr[i]
    }
}
o/p:
 array before squares is [1, 2, 3, 4, 5]
 array after squares is [1, 4, 9, 16, 25]
